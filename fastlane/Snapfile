# Snapfile for Craig-O-Clean macOS app

# IMPORTANT: snapshot only works with iOS apps
# For macOS apps, use xcodebuild test directly
# This file is kept for reference but won't be used

# The name of the scheme which contains the UI Tests
# scheme("Craig-O-Clean")

# Where should the resulting screenshots be stored?
# output_directory("./Screenshots/fastlane")

# For macOS apps, run UI tests directly:
# xcodebuild test -scheme Craig-O-Clean -destination 'platform=macOS'
